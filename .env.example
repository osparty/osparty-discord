# Copy to `.env` next to docker-compose.yml on the MAIN server and fill in the real values.
# docker compose reads this automatically and injects it into the discord-bot container.
# The real `.env` is gitignored — never commit the token.

# Shared secret the API sends as X-Internal-Token. Must match DISCORD_INTERNAL_TOKEN in the API's .env.
# Generate one with: openssl rand -hex 32
DISCORD_INTERNAL_TOKEN=change-me-to-a-long-random-string

# Bot token from the Discord Developer Portal (Bot -> Reset Token).
DISCORD_TOKEN=your-bot-token-here

# The guild the bot operates in, and the category new party channels are created under.
# Discord snowflake ids (enable Developer Mode, right-click -> Copy ID).
DISCORD_GUILD_ID=000000000000000000
DISCORD_CATEGORY_ID=000000000000000000

# Invite lifetime (seconds) and max uses (0 = unlimited).
DISCORD_INVITE_MAX_AGE_SECONDS=86400
DISCORD_INVITE_MAX_USES=0

# Host port the service listens on (private network only). Matches the API's DISCORD_SERVICE_URL port.
SERVER_PORT=8090

# In production, bind the published port to MAIN's PRIVATE IP so only the API instances can reach it.
# Leave unset locally (binds all interfaces). Example: 10.0.0.1
BOT_BIND_IP=
